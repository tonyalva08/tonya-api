{
  "name": "tonya",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "concurrently -n web,api -c blue,green \"npm:dev:web\" \"npm:dev:api\"",
    "dev:web": "npm --prefix web run dev",
    "dev:api": "npm --prefix api run dev",
    "build": "npm --prefix web run build && npm --prefix api run build",
    "lint": "npm --prefix web run lint && npm --prefix api run lint",
    "typecheck": "npm --prefix web run typecheck && npm --prefix api run typecheck",
    "test": "npm --prefix web run test && npm --prefix api run test",
    "install:all": "npm install && npm --prefix web install && npm --prefix api install"
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}
